#include<stdio.h>
int main();
{
	int j,i,n,A[50],temp;
	 printf("enter the number\n:",&n);
	 
	 printf("enter the number %d \n",n);
	 
	 for(i=0;i<n;i++)
	 {
	 	j=1;
	 	while(A[j]<A[j-1]&&j>0)
	 	{
	 		temp=
		 }
	 }
	 
}
